import React from 'react';
import cn from "classnames";

const App = () => {
    // const { theme } = useTheme();
    // const dispatch = useDispatch();

    return (
        <div>
            WEBPACK WORKS WOW
        </div>
    );
};

export default App;
